services:
  - type: web
    name: hava-durumu-uygulamasi
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn havadurumudeneme:app
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
